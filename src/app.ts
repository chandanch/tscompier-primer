type User = {
	name: string;
	age: number;
};

const samer: User = {
	name: 'Sammer Buneos',
	age: 123,
};

const dame = '';

console.log('User Details:', samer);

const showDetails = () => {
	return 'dwdw';
};
